---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: providerconfigs.mongodb.swapnil.io
spec:
  group: mongodb.swapnil.io
  names:
    categories:
    - crossplane
    - provider
    - mongodb
    kind: ProviderConfig
    listKind: ProviderConfigList
    plural: providerconfigs
    singular: providerconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProviderConfigSpec defines the desired state of ProviderConfig.
            properties:
              credentials:
                description: |-
                  ProviderCredentials holds credentials source details.
                  Supports both Kubernetes Secrets and AWS Secrets Manager.
                properties:
                  aws:
                    description: |-
                      AWS holds AWS Secrets Manager configuration
                      Used when Source is "AWS"
                    properties:
                      secretsManager:
                        description: AWSSecretsManagerReference holds configuration
                          for AWS Secrets Manager storage.
                        properties:
                          kmsKeyId:
                            type: string
                          region:
                            type: string
                          secretKey:
                            type: string
                          secretName:
                            type: string
                        required:
                        - region
                        type: object
                    type: object
                  secretRef:
                    description: |-
                      SecretRef references a Kubernetes Secret containing AWS credentials
                      Used when Source is "Secret"
                      Example secret format:
                        apiVersion: v1
                        kind: Secret
                        metadata:
                          name: backupcreds
                          namespace: crossplane-system
                        type: Opaque
                        data:
                          credentials: <base64-encoded-json>
                      Where credentials JSON contains:
                        {
                          "accessKeyId": "AKIA...",
                          "secretAccessKey": "...",
                          "sessionToken": "..."
                        }
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  source:
                    description: Source specifies which credentials source to use
                      (Secret or AWS)
                    type: string
                required:
                - source
                type: object
            required:
            - credentials
            type: object
          status:
            description: ProviderConfigStatus represents the observed state of ProviderConfig.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              users:
                description: Users of this provider configuration.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
